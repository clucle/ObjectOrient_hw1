CPerson

private:
m_sName (string)
m_sNumber[11] (string) 0xx-xxxx-xxxx, 0xx-xxx-xxxx
m_sRelation (string)
m_sEmail (string)

public:
construct, destroy
get, set


CAddressBook
private:
m_nCount (int)
m_pPerson (CPerson *)

public:
AddPerson(string sName, string sNumber string Relation, string sEmail);
DelPerson(string sName = NULL, string sPhone = NULL, string Relation = NULL);
AddRelation(string sName);
DelRelation(string sName);

Load();
Save();

Search();
CallMenu();

